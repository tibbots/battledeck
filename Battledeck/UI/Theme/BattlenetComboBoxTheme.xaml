<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Selection list in the style of the other input fields. Colors like BattlenetTextBoxTheme
         (#15171C on #555555), so text field and list look the same side by side.

         A ComboBox needs more than a Border template: WPF looks in its template for
         elements with fixed names - PART_EditableTextBox (only with IsEditable), and a
         Popup that hangs on ToggleButton.IsChecked via IsDropDownOpen. Without these parts
         the control stops working, without anything reporting an error. Here the list is
         NOT editable, which is why PART_EditableTextBox is deliberately missing. -->

    <Style TargetType="ToggleButton" x:Key="BattlenetComboToggle">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="back" Background="#15171C" BorderBrush="#555555"
                            BorderThickness="1" CornerRadius="5">
                        <!-- The arrow sits on the right in a fixed column, so the text
                             next to it doesn't run underneath it. -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="22" />
                            </Grid.ColumnDefinitions>
                            <Path x:Name="arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="#8F93A0" HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="back" Property="BorderBrush" Value="#6E7280" />
                            <Setter TargetName="arrow" Property="Fill" Value="#FFFFFF" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="back" Property="BorderBrush" Value="#4A90D9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem" x:Key="BattlenetComboBoxItemTheme">
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="back" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="back" Property="Background" Value="#2A2D36" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="back" Property="Background" Value="#2F323C" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox" x:Key="BattlenetComboBoxTheme">
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource BattlenetComboBoxItemTheme}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Style="{StaticResource BattlenetComboToggle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                          RelativeSource={RelativeSource TemplatedParent}}" />
                        <!-- The chosen entry. IsHitTestVisible="False", otherwise the
                             text catches the clicks and the list doesn't open. -->
                        <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="9,0,26,0" VerticalAlignment="Center"
                                          HorizontalAlignment="Left" IsHitTestVisible="False" />

                        <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               PopupAnimation="Slide" Focusable="False">
                            <Border Background="#1B1D24" BorderBrush="#313339" BorderThickness="1"
                                    CornerRadius="5" Margin="0,2,0,0"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <ScrollViewer Style="{StaticResource BattlenetScrollViewerTheme}">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
